export const TextWrapper = ({ children }: { children: React.ReactNode }) => (
  <div className="p-6 bg-[#fef880] w-full overflow-hidden h-full flex items-center justify-center text-center">
    {children}
  </div>
);
